package com.letta.mobile

import android.app.Activity

/** No-op outside the generated Baseline Profile capture target. */
object ProfileCaptureKeyguardHelper {
    fun allowProfileCaptureLaunch(activity: Activity) = Unit
}
